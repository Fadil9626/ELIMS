# =====================================================================
# 1. POSTGRES (elims_db container)
# =====================================================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password_here
POSTGRES_DB=postgres

# =====================================================================
# 2. BACKEND API (elims_api container)
# =====================================================================
# This connects the API container to the DB container
DATABASE_URL=postgresql://postgres:password_here@db:5432/postgres

JWT_SECRET=change_this_to_a_random_secret
PORT=5000

# Allow connections from local dev and local docker prod
CORS_ORIGIN=http://localhost:5173,http://localhost:8081

# =====================================================================
# 3. FRONTEND (Vite)
# =====================================================================
# API URL for local development
VITE_API_URL=http://localhost:5000