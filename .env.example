# =====================================================================
# 1. POSTGRES (elims_db container)
# =====================================================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Database
POSTGRES_DB=postgres


# =====================================================================
# 2. BACKEND API (elims_api container)
# =====================================================================
# ⚠️ Docker service host for DB = "db"
DATABASE_URL=postgresql://postgres:Database@db:5432/postgres

# Change this in production
JWT_SECRET=supersecretkey1234567890

PORT=5000

# Allow frontend access (dev + prod)
CORS_ORIGIN=http://localhost:5173,http://localhost:8081


# =====================================================================
# 3. FRONTEND (Vite)
# =====================================================================
# Local development API URL
VITE_API_URL=http://localhost:5000
